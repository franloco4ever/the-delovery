---
import "swiper/css/navigation";
import "swiper/css/pagination";

import productsData from "../data/paquetes.json";
import { Picture } from "astro:assets";

// 1. Cargamos todas las imágenes de la carpeta de forma dinámica
const images = import.meta.glob<{ default: ImageMetadata }>(
  "/src/assets/img_packages/*.{jpeg,jpg,png,gif}",
);

// const smallPacks = productsData.filter((item) => item.size == "chico");

// IMPORTANTE: Convertimos el map en una serie de promesas para resolver las imágenes
const smallPacksWithImages = await Promise.all(
  productsData
    .filter((item) => item.size == "chico")
    .map(async (item) => {
      const imageModule = await images[item.img]();
      return { ...item, imageResolved: imageModule.default };
    }),
);
---

<section>
  <div class="mb-8 flex flex-col items-center text-center">
    <h3
      class="flex items-center gap-2 text-2xl font-bold tracking-tight text-[#1d0c0e] sm:text-3xl"
    >
      MINI <span class="text-blue-400">🎈</span>
    </h3>
    <p class="text-primary font-medium">
      Detalles pequeños, sonrisas gigantes.
    </p>
  </div>

  <div class="swiper joySwiper !pb-12 !px-1">
    <div class="swiper-wrapper !h-auto">
      {
        smallPacksWithImages.map((el, i) => {
          return (
            <div class="swiper-slide !h-auto">
              <div class="group relative flex flex-col h-full overflow-hidden rounded-xl bg-surface-light shadow-sm hover:shadow-pop transition-all border-2 border-gold/30">
                <div class="aspect-square w-full overflow-hidden bg-gray-200">
                  <Picture
                    src={el.imageResolved}
                    alt={"media"}
                    formats={["webp", "png"]}
                    widths={[400, 800]}
                    sizes="(max-width: 800px) 100vw, 800px"
                    class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-110"
                  />
                </div>
                <div class="flex flex-1 flex-col p-4">
                  {/* <div class="mb-2 flex flex-wrap gap-1">
                    <span class="inline-flex items-center rounded-md bg-teal-accent/10 px-1.5 py-0.5 text-[10px] font-bold text-teal-700 dark:text-teal-300">
                      🍪 Cookies
                    </span>
                    <span class="inline-flex items-center rounded-md bg-teal-accent/10 px-1.5 py-0.5 text-[10px] font-bold text-teal-700 dark:text-teal-300">
                      🥛 Milk
                    </span>
                  </div> */}
                  <h4 class="mb-1 text-lg font-bold leading-tight text-gray-900 first-letter:capitalize">
                    {el.name}
                  </h4>
                  <ul class="mb-4 text-sm text-gray-600 list-disc ml-4">
                    {el.content.map((el) => {
                      return <li>{el}</li>;
                    })}
                  </ul>
                  <p class="mb-4 text-sm font-semibold text-gray-500">
                    {el.price.toLocaleString("es-MX", {
                      currency: "MXN",
                      style: "currency",
                    })}
                  </p>
                  <button
                    data-item-id={el.id}
                    data-item-name={el.name}
                    data-item-image={el.imageResolved.src}
                    data-item-price={el.price}
                    class="add-to-cart cursor-pointer mt-auto flex w-full items-center justify-center gap-2 rounded-lg bg-whatsapp py-2.5 text-sm font-bold text-white transition-colors group-hover:bg-whatsapp-hover"
                  >
                    <span class="material-symbols-outlined text-[18px]">
                      chat
                    </span>
                    LO QUIERO
                  </button>
                </div>
              </div>
            </div>
          );
        })
      }
    </div>
    <div class="swiper-pagination"></div>
    <div class="swiper-button-next hidden sm:flex"></div>
    <div class="swiper-button-prev hidden sm:flex"></div>
  </div>
</section>
<script>
  import Swiper from "swiper";
  import { Pagination, Navigation } from "swiper/modules";

  document.addEventListener("DOMContentLoaded", function () {
    // Joy Drops Swiper
    new Swiper(".joySwiper", {
      modules: [Pagination, Navigation],
      slidesPerView: 1,
      spaceBetween: 20,
      pagination: {
        el: ".swiper-pagination",
        clickable: true,
      },
      navigation: {
        nextEl: ".swiper-button-next",
        prevEl: ".swiper-button-prev",
      },
      breakpoints: {
        640: {
          slidesPerView: 2,
          spaceBetween: 20,
        },
        1024: {
          slidesPerView: 4,
          spaceBetween: 20,
        },
      },
    });
  });
</script>
